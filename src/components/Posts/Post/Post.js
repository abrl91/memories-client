import {Fragment} from "react";
import useStyles from './styles';

const Post = () => {
    const classes = useStyles();
    return <Fragment>
        <p>post</p>
    </Fragment>
}

export default Post;
